# Bybit Credentials
bybit_key: str = "<insert>"
bybit_secret: str = "<insert>"
bybit_account_type: str = "<insert>"

# Pairs
bybit_symbols: dict[str, str] = {
    "<insert>": "<insert>",
    "<insert>": "<insert>",
}

# Inverse pairs
bybit_symbols: dict[str, str] = {
    "<insert>": "<insert>",
    "<insert>": "<insert>",
}
